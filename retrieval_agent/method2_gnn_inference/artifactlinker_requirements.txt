# Pinned to the verified working environment (conda env: artifact-linker, py3.10).
# Versions marked == are load-bearing / fragile; the rest are floors.

# --- core DL / graph ---
torch==2.8.0
torch-geometric==2.7.0
numpy>=1.26,<3
scipy>=1.11                 # run_joint_gnn / joint_gnn_eval (spearmanr, kendalltau)
scikit-learn>=1.3
networkx>=2.5

# --- HF ecosystem ---
huggingface-hub>=0.23
transformers>=4.40
sentence-transformers>=3.0

# --- Stage-1 LLM (prediction/ranking via litellm) ---
litellm>=1.50
google-genai>=1.0.0

# --- Stage-2 verification agent (OpenAI Agents SDK) ---
# REQUIRED for scripts/verify_attribute_agent.py. Provides ShellTool /
# ShellToolInlineSkill / ShellToolSkillReference (added in recent versions).
# NOTE: openai-agents pulls openai>=2.37 which conflicts with litellm's
# openai==2.24 pin; the env works in practice with openai 2.37 — if litellm
# breaks for your provider, run Stage-1 LLM and Stage-2 in separate envs.
openai-agents==0.17.2
openai>=2.37

# --- utils ---
tqdm>=4.60.0
requests>=2.25.0
arxiv>=1.4.0
matplotlib>=3.5.0
pyvis>=0.2.0
einops
